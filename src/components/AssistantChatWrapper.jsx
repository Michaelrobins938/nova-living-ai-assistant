import React from 'react';

const AssistantChatWrapper = () => {
  return (
    <div
      className="assistant-ui-chat-container"
      style={{
        padding: '20px',
        background: 'rgba(0, 0, 0, 0.8)',
        borderRadius: '10px',
        border: '1px solid #00FFFF',
        color: '#00FFFF',
        textAlign: 'center',
      }}
    >
      <h3>Assistant UI Component</h3>
      <p>This component is currently disabled due to missing dependencies.</p>
      <p>Please use the original chat interface instead.</p>
    </div>
  );
};

export default AssistantChatWrapper;
